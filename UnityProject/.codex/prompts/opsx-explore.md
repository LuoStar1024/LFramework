---
description: Explore an OpenSpec idea or problem using the project skill
argument-hint: idea, problem, change-name, or comparison
---

Use the `openspec-explore` skill to explore the user's idea, problem, or existing OpenSpec change.

Input from this shortcut:

```text
$ARGUMENTS
```

Rules for this shortcut:

1. Treat `$ARGUMENTS` as the exploration topic, change name, or comparison.
2. Do not duplicate the OpenSpec workflow here; follow `.codex/skills/openspec-explore/SKILL.md`.
3. If the skill is unavailable, report that the project is missing `openspec-explore`.
